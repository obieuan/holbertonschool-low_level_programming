#include <stdio.h>
/**
 * main - check the code
 * @argc: - argc
 * @argv: - argv *
 * Return: Always 0.
 */
int main(int argc, char *argv[])
{
	if (argc >= 1)
	{
		printf("%s\n", argv[0]);
	}
	return (0);
}
