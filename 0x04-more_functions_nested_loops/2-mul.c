#include "main.h"
/**
 * mul - check the code
 * @a: nuemro a multiplicar
 * @b: segundo numero a multiplicar
 * Return: multi
 */

int mul(int a, int b)
{
	int multi = 0;

	multi = a * b;
	return (multi);
}
